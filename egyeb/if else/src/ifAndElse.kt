class ifAndElse {
    fun jatekos(): String {
        val player_Name : String = "Madrigal" //nem kell a string
        return player_Name
    }
}
fun main() {
    val jatekos = ifAndElse()
    val jatekosNev =jatekos.jatekos()
    println("A hős bejelenti a jelenlété a világnak")
    println("$jatekosNev")
    var jatekosSzint = 4
    println("$jatekosSzint")
    if (jatekosSzint == 1) { //mivel nem egyenlő egyel ezért az esle ág hajtodik végre
        println("Találkozz Buborékok úrral a puha dolgok földjén.")
    } else {
        println("Keresd meg az elvarázsolt kardot.")}
    println("A hős útra kel, hogy megtalálja az elvarázsolt kardot.")
    println("Telik az idő...")
    println("A hős visszatér a küldetéséből.")
    jatekosSzint += 1
        println(jatekosSzint)}

